export interface IStudent {
  name: string
  phoneNumber: string
  fee: number
  email: string
  address: {
  street: string
  city: string
  country: string
  postalCode: string
  state: string
  number: number
  }
}
